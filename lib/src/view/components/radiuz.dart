import 'package:flutter/material.dart';

class Radiuz {
  static const thinRadius = BorderRadius.all(Radius.circular(3));
  static const smallRadius = BorderRadius.all(Radius.circular(7));
  static const mediumRadius = BorderRadius.all(Radius.circular(14));
  static const largeRadius = BorderRadius.all(Radius.circular(25));
}
